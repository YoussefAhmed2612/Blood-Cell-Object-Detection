# Blood-Cell-Object-Detection
Fine-tuning two YOLO variants for blood cell object detection. The dataset is converted to YOLO format and split 80/20 for training and testing. Models are trained (not used with pretrained weights directly), evaluated using detection metrics, and tested with visualized predicted bounding boxes on sample images.
